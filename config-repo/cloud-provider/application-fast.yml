# ===================================================================
# Spring Boot configuration.
# ===================================================================

# ===================================================================
# Full reference is available at:
# http://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
# ===================================================================

logging:
  config: "https://raw.githubusercontent.com/ztgoto/spring-cloud-sample/master/config-repo/cloud-provider/log4j2-fast.xml"
  
server:
  port: 8100
  
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8090/eureka/

test:
  message: "provier fast profile"